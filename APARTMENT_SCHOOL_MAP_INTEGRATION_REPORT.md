# Apartment–School Map Integration v1 완료 보고서

## 구현 결과

- `python main.py sync-school-data --source ../busan_school_analysis` 명령으로 공식 학교 ID 기반 조회 snapshot을 생성한다.
- 부산 Overview에 아파트(기본 ON), 초등학교(기본 OFF), 중학교(기본 OFF) 독립 레이어와 Top 10/20/30/50, 부산 전체/선택 구·군 범위를 추가했다.
- 초등학교는 `elementary_demand_score`, 중학교는 `middle_school_score`만 사용하며 두 점수를 합산하지 않는다.
- 지도 이벤트는 `apartment + internal_complex_id`, `elementary|middle + school_id` 계약으로 구분한다.
- 학교 마커 선택 후 요약 카드와 앱 내부 학교 상세 화면을 제공한다. 상세에서 지도 복귀 시 sidebar 필터와 학교 레이어 상태가 session state에 남는다.
- snapshot 누락·스키마 오류 시 학교 영역에만 안내하고 기존 아파트 기능은 계속 동작한다.

## 실제 데이터 확인

- 초등학교 점수: 302개교, 기준연도 2026, 관측 2024~2026
- 중학교: 183개교 중 유효 점수 170개교, 점수 기준연도 2025, 유효 관측 2023~2025
- 지도/상세 통합 행: 485개교, 좌표 미확인 2개교
- 초등 연도별 이력 909행, 중학교 유효 연도별 이력 509행
- manifest에는 원천 상대경로, SHA-256, 동기화 시각, 기준연도, 행 수, 좌표 제외 건수를 기록한다.

## 검증

- `python -m pytest -q`: 76 passed
- Streamlit AppTest: 기본 진입 예외 0, 초등학교 레이어 ON 예외 0, Top 30 지도 표시 30개 확인
- 학교 ID 중복 없음, 초·중 점수 분리, 안정적 동점 정렬, 부산/선택 지역 Top N 의미를 테스트했다.
- 존재하지 않는 원천으로 동기화 실패를 유발해 기존 정상 manifest가 유지됨을 확인했다.
- `git diff --check` 통과
- 학교 원천 프로젝트 파일은 수정하지 않았다.

## 남은 수동 확인

초기 통합 당시에는 Plotly 지도 포인트의 실제 브라우저 클릭 좌표를 테스트 러너가 발생시키지 못해 수동 확인 항목으로 남겼다. 아래 Visual Fix에서 지도 컴포넌트가 PyDeck으로 변경되었으며 최신 검증 범위와 제한도 아래에 갱신했다.

## School Map Marker Visual Fix (2026-09-13)

### 확인된 원인과 수정

- 기존 지도는 Plotly 6.9.0의 MapLibre 기반 `scattermap`이었다. 학교 trace에 `diamond`와 `square`를 지정했지만 비원형 symbol은 MapLibre sprite 아이콘으로 처리되어 지원 상태에 따라 검정 fallback이 발생하며, 사용자 정의 학교 SVG를 직접 넣을 수 없다.
- 학교 색에는 불투명 HEX를 사용했지만 marker opacity가 0.82로 한 번 더 적용되었다. 초등 아이콘이 작고 지도 위에서 반투명해져 시인성이 낮았다. RGBA alpha 범위 오류는 없었다.
- 기존 크기는 `8 + 12 × sqrt(score/100)`, 즉 약 8~20px 배열이었다. 배열 자체와 점수 dtype은 정상이었으나, 비원형 sprite symbol의 크기 표현 제약과 작은 범위 때문에 중학교 상위권의 작은 점수 차이가 화면에서 동일해 보였다.
- Overview 지도만 Streamlit 1.62.0이 선택 이벤트를 지원하는 PyDeck 0.9.3으로 교체했다. 아파트는 기존 가격 구간 색·세대수 크기·안정 ID를 `ScatterplotLayer`로 유지한다.
- 학교는 외부 서버나 폰트가 필요 없는 64×64 자체 포함 SVG `IconLayer`로 표시한다. 초등은 `#16834A`, 중학교는 `#7542C8`, 흰색 건물과 어두운 외곽선이며 opacity는 1.0이다.
- 학교 크기는 `20 + 36 × (score / 100)` 픽셀이다. `sizeUnits="pixels"`, `sizeScale=1`, 20~56px 제한을 사용하므로 확대·축소와 무관하게 화면 픽셀 크기가 유지된다. 결측·비숫자·무한대·0~100 범위 밖 점수는 마커에서 제외하며 0점으로 대체하지 않는다.
- PyDeck의 단위 값은 일반 Python 문자열이 accessor 식으로 직렬화되는 것을 막기 위해 `pydeck.types.String("pixels")`로 지정했고, 최종 JSON의 `sizeUnits`/`radiusUnits`가 실제 문자열인지 테스트한다.
- `apartments`, `school-elementary`, `school-middle` 레이어 모두 고유 ID와 `entity_type`/`entity_id`를 가지므로 선택 계약은 유지된다.

### 검증 범위

- 고정 크기 예제 0/25/50/75/100점 → 20/29/38/47/56px 자동 테스트
- SVG data URI, 초록/보라 색상, 학교별 크기 배열, 픽셀 단위, 레이어 ID와 선택 객체 계약 자동 테스트
- 실제 snapshot 점수는 float64, 결측 13개, 무한대 0개, 유효 점수 범위 1.0882~99.6176이며 범위 밖 값은 0개다.
- 실제 크기 예: 센텀초등학교 89.5267점 → 52.23px, 남문초등학교 89.4038점 → 52.19px, 여고초등학교 89.1628점 → 52.10px.
- 실제 크기 예: 부산국제중학교 99.6176점 → 55.86px, 브니엘예술중학교 99.3529점 → 55.77px, 센텀중학교 98.9118점 → 55.61px.
- AppTest에서 기본 화면과 초·중학교 동시 ON 렌더링 예외가 없음을 확인했다.
- headless Chrome으로 Streamlit 미리보기 캡처를 시도했으나 해당 브라우저 세션이 Streamlit WebSocket 화면을 완성하지 못해 빈 캡처만 생성되었고 산출물에서 제거했다. 따라서 실제 WebGL 아이콘 화면과 포인터 클릭을 완료했다고 주장하지 않는다. `school_marker_preview.py`에서 운영 snapshot과 분리된 0·25·50·75·100점 시각 점검 화면을 제공하며, 실제 브라우저 수동 확인은 여전히 필요하다.

이 절은 이전 Plotly 기반 남은 수동 확인 설명을 대체한다. 현재 지도 중심·줌은 PyDeck 초기 상태로 설정되지만 사용자 조작 후 Python session state로 반환되지는 않는다.

## School Marker Shape & Detail Korean UI (2026-09-13)

이 변경은 바로 위 Visual Fix의 학교 건물 SVG와 20~56px 식을 대체한다.

- 지도 엔진은 PyDeck 0.9.3의 `IconLayer`를 유지한다. Plotly 일반 산점도와 지도 symbol의 지원 차이를 다시 우회할 필요가 없고, 현재 방식은 SVG 도형·학교별 픽셀 크기·좌표 고정·객체 선택을 함께 지원한다.
- 초등학교는 전체 64×64 viewBox를 채우는 위쪽 정삼각형 SVG(`#16834A`), 중학교는 전체 viewBox를 채우는 정사각형 SVG(`#7542C8`)다. 두 도형 모두 흰색 내부 외곽선을 가지며 불투명도는 1.0이다. 문자·이모지·폰트·외부 아이콘 서버를 사용하지 않는다.
- 표시 크기는 `marker_size_px = 16 + 0.40 × score`로 변경했다. 삼각형은 전체 높이, 사각형은 한 변 길이가 16~56px가 되며 SVG 바깥 투명 여백이 없다.
- `score`는 초등 `elementary_demand_score`, 중학교 `middle_school_score`에서 생성된 기존 snapshot 공통 열을 그대로 사용한다. 화면 내 min-max나 순위를 사용하지 않는다.
- 학교 상세의 점수명, 요약 지표, 연도별 표, 그래프 축, 상태와 경고를 한글화했다. 내부 DataFrame 컬럼과 학교 ID는 변경하지 않았다.
- 초등 상세에는 총학생수·학급수·학급당 학생수·전입·전출·순전입률·학생수 변화율·추세·보정 고학년 지수·보정 동일학년군 성장률·수요 유형·자료 완전성/신뢰도를 단위와 함께 표시한다.
- 중학교 상세에는 졸업자 수, 과학고·외고/국제고·자사고 진학자 수와 **관측 진학률**, 최근 관측연도, 관측연도 수, 자료 안정성, 점수 상태와 소표본 주의를 구분해 표시한다.
- 0~1 범위인 비율은 화면에서 한 번만 100배 해 `%`로 표시하고, 결측은 `자료 없음`으로 표시한다. 연도에는 천 단위 쉼표를 넣지 않는다.

### 원인과 적용 결과

- 직전 PyDeck 구현은 학교별 `icon_size_px` 배열과 ID·좌표의 행 순서를 올바르게 전달했으며 캐시나 CSS가 크기를 덮어쓰지 않았다. 크기가 연동되지 않은 직접 원인은 요구 식과 다른 `20 + 36 × score/100` 식을 사용했고, 내부 여백이 있는 학교 건물 그림에서 바깥 아이콘 크기 변화가 실제 도형 크기 변화로 명확히 보이지 않았기 때문이다.
- 새 SVG는 viewBox 전체를 채워 투명 여백이 없고, PyDeck 직렬화 결과에서 `getSize="icon_size_px"`, `sizeUnits="pixels"`, `sizeScale=1`, 최소 16px, 최대 56px와 학교별 숫자 배열을 확인했다.
- 실제 예: 센텀초등학교 89.5267점 → 51.81px, 남문초등학교 89.4038점 → 51.76px, 여고초등학교 89.1628점 → 51.67px.
- 실제 예: 부산국제중학교 99.6176점 → 55.85px, 브니엘예술중학교 99.3529점 → 55.74px, 센텀중학교 98.9118점 → 55.56px.
- 개발용 미리보기의 0·25·50·75·100점은 각각 16·26·36·46·56px로 계산·직렬화된다. 이 데이터는 운영 snapshot에 포함하지 않는다.

### 검증

- 전체 테스트 `81 passed`; 크기식, 잘못된 점수 제외, 삼각형/사각형 SVG path, 색상, 픽셀 단위, 레이어 선택 ID, 한글 표시값과 단위를 검증했다.
- Streamlit AppTest로 초등학교 상세와 중학교 상세를 각각 열어 예외 0건, 각 2개 한글 표, 한글 점수명과 단위를 확인했다. 내부 `school_id`, `data_year`, `science_rate` 등의 변수명은 사용자 표에 노출되지 않았다.
- 실제 WebGL 브라우저 캡처와 포인터 클릭은 이 환경의 headless Streamlit 연결 제약으로 완료하지 못했다. 확대·이동 후 좌표 고정과 마커 전체 클릭, 실제 도형 픽셀 경계는 `streamlit run school_marker_preview.py` 및 운영 앱에서 수동 확인이 남아 있다.

## School Marker Scale Adjustment (2026-09-13)

- 상위 학교가 아파트 마커보다 과도하게 커 보이는 문제를 줄이기 위해 공통 설정 `SCHOOL_MARKER_SCALE = 0.5`를 추가했다.
- 최종 식은 `(16 + 0.40 × score) × 0.5`이며 0·25·50·75·100점은 각각 8·13·18·23·28px다.
- 축소는 `school_icon_size`에서 한 번만 적용한다. PyDeck의 `sizeScale`은 1로 유지하고, 렌더러 최소·최대 제한도 8px·28px로 맞춰 계산된 축소를 덮어쓰지 않게 했다.
- 삼각형·사각형의 SVG viewBox, 색상, 불투명도, 중심점과 클릭 ID는 변경하지 않았다. 아파트 `ScatterplotLayer`의 5~22px 반지름 계산도 변경하지 않았다.
- 실제 예: 센텀초등학교 89.5267점 51.81→25.91px, 남문초등학교 89.4038점 51.76→25.88px, 부산국제중학교 99.6176점 55.85→27.92px, 브니엘예술중학교 99.3529점 55.74→27.87px.
