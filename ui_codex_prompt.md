# 부산아파트데이터랩 — 부산 Overview 첫 화면 UX/UI 개편

현재 Streamlit 기반 `부산아파트데이터랩`의 초기 화면은 `부산 Overview` 메뉴이다.

현재 화면에는 다음 요소가 순서대로 표시된다.

1. 서비스 제목
2. 실거래 데이터 현황 대형 카드
   - 매매 수록기간 / 당월 건수 / 최신 수집시점
   - 전월세 수록기간 / 당월 건수 / 최신 수집시점
   - 구·군별 수집 상세
3. 지도 제목 및 기본 표시 조건 설명
4. 학교/아파트 표시 옵션
5. 마커 크기 및 색상 설정
6. 부산 아파트 지도
7. 지도 설명 및 범례

현재 구조는 데이터 관리 정보와 설명이 지도보다 먼저 크게 노출되어
Overview의 핵심인 지도 탐색이 첫 화면에서 늦게 등장하는 문제가 있다.

이번 작업에서는 기존 데이터 처리, 지도 기능, 필터 기능은 유지하되
첫 화면을 다음 원칙에 따라 재구성한다.

핵심 UX 원칙:

> **지도 먼저, 설명은 필요할 때**

사용자가 부산 Overview에 진입하면 스크롤하지 않거나 최소한의 스크롤만으로
지도 대부분을 바로 볼 수 있어야 한다.

---

## 1. 먼저 기존 코드 구조 분석

수정 전에 프로젝트 전체를 분석해서 다음 위치를 정확히 확인한다.

- 부산 Overview 페이지 파일
- 페이지 title/header 렌더링 위치
- 실거래 데이터 현황 렌더링 함수
- 매매/전월세 수집현황 생성 함수
- 구·군별 수집 상세 UI
- 지도 제목 및 설명 출력 부분
- 아파트/학교 표시 토글
- 학교 상위 N 필터
- 학교 순위 범위 필터
- 마커 크기 선택 UI
- 마커 색상 선택 UI
- Plotly 지도 생성 함수
- 지도 하단 설명 및 범례
- sidebar 생성 함수
- sidebar 메뉴와 분석 필터 렌더링 구조

기존 구조를 최대한 재사용한다.

불필요한 대규모 리팩터링은 하지 않는다.

---

## 2. 최종 목표 화면 구조

부산 Overview 첫 화면을 다음 순서로 구성한다.

```text
[서비스 제목]

열심남의 부산 아파트 데이터랩

실거래와 단지정보를 결합한 부산 아파트 분석 도구입니다.
투자 추천 또는 매수 신호가 아니라는 기존 안내는 유지해도 된다.

[간단한 데이터 상태 한 줄]

최종 업데이트 2026.09.17 10:46 KST
· 매매 913건
· 전월세 1,338건
· 16/16개 구·군
[ⓘ 데이터 현황]

[부산 아파트 지도]

[필터 / 표시 설정]

지도

[지도 하단 간단 범례]

크기: 세대수 · 색상: 가격
○ 652    ◯ 853    ◉ 1,208세대

[ⓘ 지도 읽는 법]
```

즉 첫 화면에서 반드시 다음 순서가 되어야 한다.

```text
제목
→ 아주 짧은 데이터 상태
→ 지도 컨트롤
→ 지도
```

현재처럼 데이터 현황 대형 카드가 지도를 밀어내지 않도록 한다.

---

## 3. "실거래 데이터 현황" 대형 카드 제거

현재 다음 내용을 크게 보여주는 카드 영역을 기본 화면에서 제거한다.

- 매매
  - 수록 기간
  - 당월 건수
  - 최신 수집시점

- 전월세
  - 수록 기간
  - 당월 건수
  - 최신 수집시점
  - 전세/월세 건수

- 구·군별 수집 상세

기존 데이터를 삭제하는 것이 아니다.

메인 화면에서는 요약 한 줄만 표시한다.

예:

```text
데이터 업데이트 2026.09.17 10:46 KST
· 매매 913건
· 전월세 1,338건
· 16/16개 구·군 ✓
ⓘ 데이터 현황
```

가능하면 다음과 같이 구현한다.

```python
status_col, info_col = st.columns([8, 2])

with status_col:
    st.caption(
        "데이터 업데이트 2026.09.17 10:46 KST · "
        "매매 913건 · 전월세 1,338건 · "
        "16/16개 구·군 ✓"
    )

with info_col:
    with st.popover("ⓘ 데이터 현황"):
        ...
```

단 실제 값은 하드코딩하지 말고
현재 프로젝트에서 사용 중인 데이터 상태 변수에서 가져온다.

---

## 4. 데이터 현황 상세는 popover로 이동

`ⓘ 데이터 현황` 클릭 시 기존 상세 정보를 확인할 수 있도록 한다.

Popover 안에는 다음 정보를 넣는다.

예:

### 매매

- 수록기간: 2020.01 ~ 2026.09
- 최신월 반영건수: 913건
- 최신 수집: 2026.09.17 10:46 KST
- 구·군 수집 현황: 16/16

### 전월세

- 수록기간: 2020.01 ~ 2026.09
- 최신월 반영건수: 1,338건
- 전세 768건
- 월세 570건
- 최신 수집: 2026.09.17 10:46 KST

하단 안내:

> 계약일 기준 자료입니다.  
> 신고 지연 및 정정·취소 반영에 따라 최근 월과 과거 월의 거래 건수가 변경될 수 있습니다.

기존의 "구·군별 수집 상세"도 이 popover 안으로 이동한다.

구·군별 정보가 길 경우:

```python
with st.expander("구·군별 수집 상세"):
    ...
```

형태로 popover 내부에서 다시 접어 보여준다.

---

## 5. 메인 화면에서 기술 설명 제거

현재 지도 위 또는 지도 주변에 다음과 같은 긴 설명이 노출되어 있다.

예:

- 현재 표현: 크기 = 세대수 · 색상 = 가격
- 크기는 2~98% 구간을 기준으로 제곱근 변환해 5~26px로 표시
- 최근 12개월 거래금액 산정기간
- canonical key `internal_complex_id`
- 시가총액은 KB 보정 시가총액 기준
- Local Value Gap 데이터 부재
- 학교 위치는 배정학교를 의미하지 않음
- 초록 삼각형 / 보라 사각형 설명
- 초·중 점수 직접 비교 불가
- 8억원 이상 색상 구분 방식
- 크기 범례 문장

이 내용들은 정확하지만
Overview의 기본 화면에서는 너무 많은 설명이므로 제거한다.

다만 정보 자체는 삭제하지 말고
`ⓘ 지도 읽는 법`으로 이동한다.

---

## 6. 지도 제목 단순화

현재:

> 조건에 맞는 아파트 위치

를 다음처럼 더 단순하게 변경한다.

추천:

> **부산 아파트 지도**

또는 현재 디자인 아이콘을 유지한다면:

> **🗺 부산 아파트 지도**

제목 아래의 긴 문장:

> 기본 표시 조건: 500세대 이상 · 사용승인 2001년 이후 · 구·군/법정동 전체

는 제거한다.

대신 현재 적용 중인 조건을 badge/chip 형태에 가깝게 표현한다.

예:

```text
[500세대+] [2001년 이후] [부산 전체]
```

Streamlit에서 실제 chip 컴포넌트가 어렵다면
작은 caption 또는 HTML/CSS badge로 구현해도 된다.

필터 값이 변경되면 실제 상태에 맞게 동적으로 변경한다.

---

## 7. 지도 컨트롤 재배치

현재 기능은 유지한다.

### 1단 — 표시 대상

- 아파트
- 초등학교
- 중학교

학교 관련 옵션:

- 학교 상위 N
- 학교 순위 범위

### 2단 — 아파트 마커 표시 기준

마커 크기:

- 세대수
- 시가총액
- 최근 12개월 거래금액

마커 색상:

- 가격
- School Value Gap

현재 Local Value Gap 데이터가 프로젝트에 없다면
선택지에 억지로 추가하지 않는다.

권장 레이아웃:

```text
표시
[아파트 ON] [초등학교 OFF] [중학교 OFF]

학교
상위 [30 ▼]     순위범위 [부산전체 ▼]

마커 크기
[세대수] [시가총액] [최근12개월 거래금액]

마커 색상
[가격] [School Value Gap]
```

Desktop에서는 가능한 한 2줄 또는 3줄 이내에서 끝나도록 한다.

지도를 아래로 지나치게 밀어내면 안 된다.

모바일에서는 자연스럽게 줄바꿈되어도 된다.

---

## 8. 지도는 가능한 빨리 시작

이번 개편에서 가장 중요한 acceptance criterion이다.

현재 화면보다 지도가 훨씬 위에서 시작해야 한다.

목표:

**Desktop 1440p 기준 페이지 상단에서 약 250~320px 이내에 지도 상단이 보이도록 한다.**

대략적인 수직 구조:

```text
Title         약 60~80px
Status        약 25~35px
지도 제목      약 35~45px
Controls      약 100~130px
```

그 후 바로 지도.

불필요한 큰 vertical spacer,
빈 `st.write("")`,
과도한 `st.markdown("<br>")`,
큰 margin/padding을 제거한다.

---

## 9. 지도 높이 확보

Overview의 핵심 콘텐츠는 지도이므로
첫 화면에서 지도 면적이 충분히 보여야 한다.

Desktop 기준 Plotly map 높이는
기존 값이 너무 작다면 약 **620~720px** 수준을 검토한다.

예:

```python
fig.update_layout(
    height=680
)
```

단 기존 화면 크기와 모바일 레이아웃을 확인해서
과도한 높이는 피한다.

최종 기준은:

> **첫 화면에서 지도 존재감이 가장 커야 한다.**

---

## 10. 지도 하단 상태 표현

현재 선택된 시각화 기준을
지도 바로 아래 또는 지도 상단 오른쪽에 간단히 표시한다.

예:

```text
크기: 세대수 · 색상: 가격
```

Streamlit:

```python
st.caption(
    f"크기: {marker_size_mode} · 색상: {marker_color_mode}"
)
```

기존처럼 긴 설명은 붙이지 않는다.

---

## 11. 마커 크기 범례 개선

현재:

> 크기 범례(25% · 중앙 · 75%): 652세대 · 853세대 · 1,208세대

형태의 문장은 시각적으로 약하다.

이를 간단한 bubble legend 형태로 개선한다.

예:

```text
마커 크기 · 세대수

○ 652      ◯ 853      ◉ 1,208
 25%         중앙         75%
```

시가총액 선택 시:

```text
○ 2,800억    ◯ 4,900억    ◉ 8,600억
   25%          중앙          75%
```

최근 12개월 거래금액 선택 시:

```text
○ 90억       ◯ 210억      ◉ 520억
```

기존에 이미 25% / median / 75% 계산값이 있다면 재사용한다.

없다면 현재 지도에 사용되는 filtered dataframe을 기준으로 계산한다.

```python
q25 = values.quantile(0.25)
q50 = values.quantile(0.50)
q75 = values.quantile(0.75)
```

---

## 12. "지도 읽는 법" popover

지도 하단 또는 지도 제목 근처에:

`ⓘ 지도 읽는 법`

을 둔다.

Popover 안의 내용은 사용자 관점으로 정리한다.

### 아파트

- 원의 크기 → 선택한 규모 지표
- 원의 색상 → 선택한 가격/분석 지표
- 마커 클릭 → 아파트 상세 이동

### 마커 크기

- 세대수 → 단지의 물리적 규모
- 시가총액 → 단지의 자산가치 규모
- 최근 12개월 거래금액 → 최근 시장 유동성

### 학교

- 초록 삼각형 → 초등학교 수요점수
- 보라 사각형 → 중학교 진학성과 점수
- 점수가 높을수록 마커가 크게 표시됨
- 지도상 인접학교는 해당 아파트의 실제 배정학교를 의미하지 않음
- 초등학교와 중학교 점수는 서로 다른 지표이므로 점수 자체를 직접 비교하지 않음

### 가격 색상

- 현재 사용 중인 기존 가격 색상 구간 설명
- 8억원 이상 고가 단지는 기존 구현대로 1억원 단위 구분

---

## 13. 기술적 산정 기준은 2단계 아래로 이동

일반 사용자가 볼 필요가 없는 다음 기술정보는
`지도 읽는 법` 안의 expander에 넣는다.

예:

```python
with st.expander("산정 기준 자세히 보기"):
    ...
```

여기에 다음 내용을 넣는다.

- 마커 크기는 2~98 percentile 구간 clipping
- sqrt transformation
- 5~26px scaling
- 최근 12개월 거래기간 계산기준
- 시가총액 source
- `internal_complex_id`
- 가격 색상 상세 기준

즉 UI 정보 계층을 다음처럼 한다.

```text
1단계:
지도

2단계:
지도 읽는 법

3단계:
산정 기준 자세히 보기
```

`internal_complex_id` 같은 개발/데이터 key는
기본 화면에 절대 노출하지 않는다.

---

## 14. Local Value Gap 관련 문구 제거

현재:

> Local Value Gap 산출 데이터는 현재 프로젝트에 없어 색상 선택에서 제외했습니다.

같은 개발 상태 메시지가 기본 사용자 화면에 보인다면 제거한다.

이는 사용자 정보가 아니라 개발 상태 정보다.

코드 주석 또는 개발용 로그로 남긴다.

Local Value Gap이 실제 추가되기 전까지 UI에서 언급하지 않는다.

---

## 15. 사이드바 최근 업데이트 표시

현재 메인 화면에서 데이터 현황을 축소하는 대신
sidebar 하단에 최신 업데이트 시점을 작게 표시한다.

예:

```text
─────────────

데이터 업데이트
2026.09.17 10:46 KST
```

가능하면:

```python
st.sidebar.divider()
st.sidebar.caption("데이터 업데이트")
st.sidebar.caption(latest_update_text)
```

형태로 구현한다.

메인 화면의 업데이트 한 줄과
동일한 데이터 source를 사용한다.

중복 계산 또는 하드코딩하지 않는다.

---

## 16. 사이드바 중복 렌더링 버그 확인

현재 화면 캡처에서는 sidebar 하단에 다음 항목이 반복 렌더링되는 문제가 보인다.

- 분석 필터
- 노후단지 Watch

가 여러 차례 반복되고 있다.

이 현상이 실제 코드에서도 발생하는지 반드시 확인한다.

다음 가능성을 점검한다.

- sidebar rendering 함수가 반복 호출되는지
- 메뉴 loop 내부에서 sidebar filter를 만드는지
- page별 filter renderer가 모두 실행되는지
- `st.sidebar` 코드가 여러 module import 시 실행되는지
- session rerun 시 누적되는 구조인지
- 동일 UI 함수가 여러 위치에서 호출되는지

정상적인 sidebar는 다음과 같이 한 번만 렌더링되어야 한다.

```text
시장 분석
- 부산 Overview
- 거래량 TOP 20
- 아파트 TOP 20
- 아파트 시가총액
- 실거래가 단지 검색

지역 분석
- 구군 비교
- 동 비교

아파트 분석
- 아파트 상세
- 학교 상세
- 아파트 비교

Watch
- 기존 Watch 메뉴들

하단
- 데이터 업데이트
```

반복되는 `분석 필터` 블록이 의도된 것이 아니라면 제거한다.

단 다른 페이지에서 필요한 sidebar filter가 있다면
현재 활성화된 페이지에서만 1회 렌더링하도록 한다.

---

## 17. 현재 지도 기능은 변경하지 않음

이번 작업은 UX/UI 개선이 중심이다.

다음 기존 로직은 특별한 문제가 없는 한 수정하지 않는다.

- apartment filtering
- school filtering
- map center
- zoom level
- marker click
- apartment detail 이동
- school summary card
- marker size calculation
- marker color calculation
- KB 보정 시가총액
- 12개월 거래금액 계산
- School Value Gap
- Plotly trace
- 데이터 join
- canonical key

특히 숫자 계산 결과가 UI 개편으로 달라져서는 안 된다.

---

## 18. 기존 데이터 현황 함수 재사용

기존 실거래 데이터 현황 함수가 있다면 삭제하지 않는다.

예:

```text
render_transaction_status()
render_collection_status()
build_collection_summary()
```

등이 있다면

대형 card rendering 부분과
데이터 생성 로직을 분리한다.

추천:

```python
status = get_transaction_status()

render_compact_status(status)

with st.popover("ⓘ 데이터 현황"):
    render_transaction_status_detail(status)
```

즉 data logic을 다시 만들지 않고 presentation만 변경한다.

---

## 19. Streamlit rerun 성능

UI 변경으로 불필요한 데이터 재로딩이 발생하면 안 된다.

특히:

- popover open
- marker size 변경
- marker color 변경
- school toggle

시 전체 실거래 데이터를 반복 로딩하지 않도록 한다.

기존 `@st.cache_data` 구조를 유지 또는 활용한다.

---

## 20. 스타일

전체적인 디자인은 현재 앱의

- 흰색 배경
- 붉은 accent
- 얇은 border

스타일을 유지한다.

새로운 강한 색상이나 카드 디자인을 추가하지 않는다.

방향:

- 최소한의 카드
- 얇은 divider
- caption 활용
- compact control
- 지도 우선
- 공백을 과도하게 사용하지 않음

정보 밀도는 줄이되
전문 분석 도구의 느낌은 유지한다.

---

## 21. 최종 예상 화면

대략 다음과 같은 모습이 되어야 한다.

```text
🏢 열심남의 부산 아파트 데이터랩
실거래와 단지정보를 결합한 부산 아파트 분석 도구입니다.

데이터 업데이트 2026.09.17 10:46 KST
· 매매 913건 · 전월세 1,338건 · 16/16개 구·군 ✓
                                            [ⓘ 데이터 현황]

🗺 부산 아파트 지도
[500세대+] [2001년 이후] [부산 전체]

표시
[● 아파트] [○ 초등학교] [○ 중학교]

학교 상위 [30 ▼]              순위범위 [부산 전체 ▼]

마커 크기
[세대수] [시가총액] [최근 12개월 거래금액]

마커 색상
[가격] [School Value Gap]

┌─────────────────────────────────────────┐
│                                         │
│                 부산 지도                │
│                                         │
│                                         │
└─────────────────────────────────────────┘

크기: 세대수 · 색상: 가격

마커 크기
○ 652       ◯ 853        ◉ 1,208세대
 25%          중앙           75%

[ⓘ 지도 읽는 법]
```

---

## 22. Desktop / Mobile 확인

Desktop과 mobile을 모두 확인한다.

### Desktop

- 컨트롤이 너무 세로로 길어지지 않을 것
- 첫 화면에서 지도 상당 부분이 보일 것

### Mobile

- column overflow가 없을 것
- radio/button이 자연스럽게 wrap될 것
- 지도 너비가 container에 맞을 것
- popover가 화면 밖으로 나가지 않을 것

---

## 23. 접근성 / 설명

ⓘ 아이콘만 사용해서 의미가 불분명할 수 있으므로
가능하면 다음처럼 텍스트를 함께 쓴다.

- `ⓘ 데이터 현황`
- `ⓘ 지도 읽는 법`

tooltip만 있는 작은 아이콘 하나보다는
클릭 가능한 문구 형태가 좋다.

---

## 24. 테스트

다음 항목을 반드시 확인한다.

### 초기 화면

- 부산 Overview 진입
- 대형 실거래 데이터 카드가 사라졌는지
- 업데이트 한 줄이 표시되는지
- 지도 시작 위치가 기존보다 위로 올라갔는지

### 데이터 현황

- popover 정상 동작
- 매매 데이터 정상
- 전월세 데이터 정상
- 구·군 상세 정상

### 지도

- 아파트 토글 정상
- 초등학교 토글 정상
- 중학교 토글 정상
- 학교 상위 N 정상
- 학교 범위 정상

### 마커

- 세대수 정상
- 시가총액 정상
- 최근 12개월 거래금액 정상
- 가격 색상 정상
- School Value Gap 정상

### 클릭

- 아파트 상세 이동 정상
- 학교 카드 정상

### Sidebar

- 메뉴 중복 없음
- 분석 필터 중복 없음
- 노후단지 Watch 중복 없음
- 데이터 업데이트 시점 정상

---

## 25. 회귀 테스트

기존 테스트가 있다면 전체 수행한다.

UI 변경으로 기존 분석값이나 데이터가 변하면 안 된다.

특히 다음 값은 변경 전후 동일해야 한다.

- 표시 대상 아파트 수
- 세대수
- 시가총액
- 최근 12개월 거래금액
- 대표가격
- School Value Gap
- 학교 점수
- 지도 좌표

---

## 26. 완료 후 보고

구현 완료 후 다음 형식으로 결과를 보고한다.

### 변경 파일

- 파일
- 변경 내용

### UI 변경 전/후

- 제거한 요소
- 축소한 요소
- popover로 이동한 요소

### 지도 시작 위치

가능하면 대략적인 vertical space 감소량 설명

### Sidebar

- 반복 렌더링 원인
- 수정 여부

### 기존 기능 회귀

- 정상 동작 여부

### 테스트

- 실행한 테스트
- PASS / FAIL

### 미해결 사항

있는 경우만 작성

---

# 최종 목표

부산 Overview는 **"데이터 설명 페이지"가 아니라 "부산 아파트 시장을 탐색하는 지도형 대시보드"**가 되어야 한다.

사용자의 기본 행동 흐름은 다음과 같아야 한다.

```text
앱 진입
→ 바로 지도 확인
→ 마커 크기/색상 변경
→ 관심 지역 탐색
→ 아파트 클릭
→ 필요할 때만 데이터 기준 및 산정방식 확인
```

따라서 메인 화면에서는

- 설명보다 지도
- 관리정보보다 탐색
- 기술정보보다 실제 시장 구조

가 먼저 보여야 한다.

기존 데이터와 분석 로직은 유지하고
정보의 시각적 우선순위만 재설계한다.

---

## 구현 시 추가 원칙

이번 작업에서는 `st.popover()`를 적극 활용하되
`st.expander()`를 메인 화면에 여러 개 배치하지 않는다.

`st.expander()`는 닫혀 있어도 제목 줄이 계속 수직 공간을 차지하므로
이번 UX 개선 목적에는 `st.popover()`가 더 적합하다.

단, `ⓘ 데이터 현황` 또는 `ⓘ 지도 읽는 법` popover 내부에서
긴 세부 내용을 다시 접어 보여줘야 하는 경우에는
`st.expander()`를 보조적으로 사용해도 된다.
